export interface UseSigningServiceProps {
  props: any;
}

const useSigningService = (props: UseSigningServiceProps) => {
  // use the hashgraph sdk to sign transactions
  return;
};

export { useSigningService };
