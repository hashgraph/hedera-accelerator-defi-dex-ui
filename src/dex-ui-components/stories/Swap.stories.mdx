<!-- SwapTokens.stories.mdx -->

import { Meta, Story, Canvas } from "@storybook/addon-docs";

import { SwapTokens } from "..";

<Meta title="SwapTokens" component={SwapTokens} />

export const Template = (args) => <SwapTokens {...args} />;

# SwapTokens Component

With `MDX`, we can define a story for `SwapTokens` right in the middle of our
Markdown documentation.

<Canvas>
  <Story name="SwapTokens">
    <Swap />
  </Story>
</Canvas>
