<!-- Swap.stories.mdx -->

import { Meta, Story, Canvas } from "@storybook/addon-docs";

import { Swap } from "../components";

<Meta title="Swap" component={Swap} />

export const Template = (args) => <Swap {...args} />;

# Swap Component

With `MDX`, we can define a story for `Swap` right in the middle of our
Markdown documentation.

<Canvas>
  <Story name="swap">
    <Swap />
  </Story>
</Canvas>
